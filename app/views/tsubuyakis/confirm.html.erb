<h3>以下の内容で、送信する。</h3>

<%= form_with(model: @tsubuyaki, url: tsubuyakis_path, local: true) do |form| %>
  <%= form.hidden_field :content %>
  <p>本文:<%= @tsubuyaki.content %></p><br>

  <%= form.submit "登録" %>
  <%= form.submit "戻る", name: 'back' %>

<% end %>